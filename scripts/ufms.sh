# Description: Utility functions meant to clean-up and organize the current working directory where it runs.

# This script will run python CLI meant to clean-up and organize the current working directory
# where it runs.

# It will have a user configuration file ~./.ufmsrc will contain configuration selections for the user to choose
# things like workspace directory root path and style (flat or nested), file organization rules

# It will check for the update status for ~/w